# Copy this file to ".env" and fill in your key. ".env" is gitignored — it never gets committed.
# Get a key at https://console.anthropic.com  (free starter credits; $5 minimum prepay after that).
ANTHROPIC_API_KEY=your-key-here
